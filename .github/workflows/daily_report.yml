name: Daily Email Report

# Sends the bifurcated Excel report by email every day at 17:22 IST (11:52 UTC).
# Requires repo secrets: GMAIL_ADDRESS, GMAIL_APP_PASSWORD, REPORT_EMAIL_TO.

on:
  schedule:
    - cron: '52 11 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: daily-email-report
  cancel-in-progress: false

jobs:
  report:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests==2.34.2 openpyxl==3.1.5

      - name: Build and email report
        env:
          GMAIL_ADDRESS: ${{ secrets.GMAIL_ADDRESS }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          REPORT_EMAIL_TO: ${{ secrets.REPORT_EMAIL_TO }}
        run: python daily_report.py

      - name: Persist report state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reported.json
          # Commit only when the staged file actually changed; untracked
          # leftovers (e.g. the generated xlsx) must not force a commit.
          if ! git diff --cached --quiet; then
            git commit -m "Update daily-report state [skip ci]"
            for i in 1 2 3 4; do
              git pull --rebase origin main && git push && break
              sleep $((2 ** i))
            done
          fi
